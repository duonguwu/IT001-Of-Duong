#include <iostream>
using namespace std;

struct DATHUC
{
    double heso;
    int somu;
};
void Xuat(DATHUC B){
    int a[100];
    for (int i = 0; i < n)
}
DATHUC Nhap(){
    DATHUC B;
    int n;
    cin >> n;
    for (int i = 0; i < n; i++){
        cin >> B.heso >> B.somu;
    }
}



int main() {
    DATHUC B; B = Nhap();
    cout << "Da thuc vua nhap la: "; Xuat(B);
    double x; cin >> x;
    // cout << "\nVoi x=" << x << ", gia tri da thuc la: "
    //      << setprecision(2) << fixed << TinhDaThuc(B, x);
    return 0;
}




    // if (a.heso != 0) {
    //     if (a.heso == 1) cout << "x^" << a.somu;
    //     else if (a.heso == -1) cout << "-x^" << a.somu;
    //     else if (a.somu == 0) cout << 1;
    //     else if (a.somu == 1) cout << a.heso << "x";
    //     else {
    //         if (a.heso > 0) cout << "+" <<a.heso << "x^" << a.somu;
    //         if (a.heso > 0) cout << "-" << a.heso << "x^" << a.somu;
    //     }
    // }